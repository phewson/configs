#+TITLE: Emacs Configuration
#+PROPERTY: header-args:emacs-lisp :tangle ~/.emacs.d/init.el :mkdirp yes

* Overview
This is my Emacs configuration.

- Managed with straight.el
- Literate config using Org mode
- Tangled with =C-c C-v t=

Goal:
- Minimal, reproducible setup
- Focus on R, LaTeX, SQL, and programming

  * Package Management (straight.el)

We disable package.el and use straight.el for reproducibility.

#+BEGIN_SRC emacs-lisp
(setq package-enable-at-startup nil)

(defvar bootstrap-version)
(let ((bootstrap-file
       (expand-file-name "straight/repos/straight.el/bootstrap.el"
                         user-emacs-directory))
      (bootstrap-version 6))
  (unless (file-exists-p bootstrap-file)
    (with-current-buffer
        (url-retrieve-synchronously
         "https://raw.githubusercontent.com/radian-software/straight.el/develop/install.el"
         'silent 'inhibit-cookies)
      (goto-char (point-max))
      (eval-print-last-sexp)))
  (load bootstrap-file nil 'nomessage))

(straight-use-package 'use-package)
(setq straight-use-package-by-default t)

(eval-when-compile
  (require 'use-package))
#+END_SRC

* Appearance

Simple, readable, distraction-free setup.

#+BEGIN_SRC emacs-lisp
(load-theme 'modus-operandi t)
(set-frame-font "DejaVu Sans Mono-11" nil t)

(setq inhibit-splash-screen t
      use-dialog-box nil)

(tool-bar-mode -1)
(column-number-mode t)

(setq-default line-spacing 0.4)
#+END_SRC

* Core Behaviour

#+BEGIN_SRC emacs-lisp
(prefer-coding-system 'utf-8-unix)

(save-place-mode 1)
(global-auto-revert-mode 1)
(setq global-auto-revert-non-file-buffers t)

(recentf-mode 1)
(setq recentf-max-menu-items 25)

(global-set-key (kbd "C-x C-r") #'recentf-open-files)
#+END_SRC

* Completion

Modern minimal completion stack.

#+BEGIN_SRC emacs-lisp
(use-package vertico
  :init (vertico-mode))

(use-package orderless
  :custom
  (completion-styles '(orderless basic)))

(use-package marginalia
  :init (marginalia-mode))

(use-package consult)

(use-package corfu
  :init (global-corfu-mode))
#+END_SRC

* Environment

These are Emacs-specific environment variables.

#+BEGIN_SRC emacs-lisp
(setenv "DATASTORE" "/home/phewson/DATA")
(setenv "PGUSER" "pgdocker")
(setenv "PGHOST" "localhost")
(setenv "PGDATABASE" "official")
(setenv "PGPORT" "15432")

(use-package direnv
  :config (direnv-mode))
#+END_SRC

* Navigation & Editing

#+BEGIN_SRC emacs-lisp
(windmove-default-keybindings)

(use-package pulsar
  :config (pulsar-global-mode 1))

(use-package writeroom-mode
  :custom (writeroom-width 100)
  :hook (writeroom-mode . (lambda ()
                            (display-line-numbers-mode
                             (if writeroom-mode -1 1)))))

(global-set-key (kbd "<f9>") #'writeroom-mode)

(setq dired-kill-when-opening-new-dired-buffer t)
(setq dired-listing-switches "-lah --group-directories-first")
#+END_SRC

* Programming

#+BEGIN_SRC emacs-lisp
(use-package eglot
  :hook ((ess-r-mode . eglot-ensure)
         (python-mode . eglot-ensure)
         (c++-ts-mode . eglot-ensure)
         (LaTeX-mode . eglot-ensure)))

(setq major-mode-remap-alist
      '((c++-mode . c++-ts-mode)
        (python-mode . python-ts-mode)))

(use-package rainbow-delimiters
  :hook (prog-mode . rainbow-delimiters-mode))

(use-package flycheck
  :init (global-flycheck-mode))

(add-hook 'text-mode-hook #'flyspell-mode)
#+END_SRC

* R / ESS

#+BEGIN_SRC emacs-lisp
(use-package ess
  :init (require 'ess-site)
  :config
  (setq ess-indent-offset 2))
#+END_SRC

* SQL

#+BEGIN_SRC emacs-lisp
(setq sql-product 'postgres)

(setq sql-postgres-login-params
      '((user :default "pgdocker")
        (database :default "official")
        (server :default "localhost")
        (port :default 15432)))
#+END_SRC

* LaTeX

#+BEGIN_SRC emacs-lisp
(use-package tex
  :straight auctex
  :hook (LaTeX-mode . (lambda ()
                        (turn-on-reftex)
                        (flyspell-mode)
                        (TeX-fold-mode)))
  :config
  (setq TeX-auto-save t
        TeX-parse-self t
        TeX-source-correlate-mode t))

(use-package cdlatex)
(use-package xenops)
#+END_SRC

* Utilities

#+BEGIN_SRC emacs-lisp
(use-package magit)
(use-package docker)
(use-package json-mode)
(use-package stan-mode)

(use-package vundo
  :bind (("C-x u" . vundo)))

(use-package browse-kill-ring
  :bind (("M-y" . browse-kill-ring)))
#+END_SRC

* Icons

NOTE: Run =M-x nerd-icons-install-fonts= after install.

#+BEGIN_SRC emacs-lisp
(use-package nerd-icons)
(use-package nerd-icons-dired
  :hook (dired-mode . nerd-icons-dired-mode))
#+END_SRC

* Org Mode

Keep Org config separate for clarity.

#+BEGIN_SRC emacs-lisp
(load-file "~/configs/emacs/.orgconfigs.el")
#+END_SRC
